Review the `5r6j4d` sync-result contract after operator steering and adjacent review loops settled the dirty-policy, setup/overlay, and init ownership boundaries.

The review should stay narrow to one question: how should `kstoolchain sync` present its own truthful action result in text and JSON without collapsing back into the broad `status` truth surface?

Relevant settled boundaries:
- `sync` stays narrow on the ready set.
- `status` remains the broad inspection surface.
- dirty-policy and persisted provenance truth are already owned by `a2bwaz`.
- setup/overlay truth and `SETUP_BLOCKED` are already owned by `gqksyd`.
- `sync` remains the canonical day-two command under `1vqbne`.

The pack should include the active `5r6j4d` stone, the smallest adjacent doctrine slice that shapes judgment, and the exact code seams showing that `sync` already behaves differently from `status` but still renders like it.
