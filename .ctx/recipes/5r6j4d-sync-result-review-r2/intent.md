Review the revised `5r6j4d` stone after round one froze the direction but not yet the full contract.

This round is narrow. The open question is whether the proposed `SyncReport` contract is now explicit enough to implement without downstream invention.

Focus on:
- the dedicated top-level sync schema
- the outcome vocabulary and `ok` / exit mapping
- terminal truth being derived from the post-reload ready-set projection
- the result-versus-error boundary
- JSON stability under `--verbose`
- delegated `init` alignment with the new sync contract

Do not reopen dirty-policy, setup/overlay, rollout, or init-vs-sync ownership doctrine unless the current proposal directly contradicts them.
